{% extends 'layout.twig' %}

{% block title post.name %}

{% block body %}

<h2>{{ post.name }}</h2>

<p class="text-muted">{{ post.created_at | ago }}</p>

    {{ post.content | nl2br }}

{% endblock %}
